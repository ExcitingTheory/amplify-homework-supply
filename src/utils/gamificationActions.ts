/**
 * @deprecated This module has been replaced by app/actions/gamification.ts (server actions).
 * This stub exists to prevent import resolution failures in legacy tests.
 */

export async function awardXPAndCheck(..._args: any[]): Promise<any> {
  throw new Error(
    "gamificationActions has been moved to app/actions/gamification.ts",
  );
}

export async function generateSkillTreeFromUnit(..._args: any[]): Promise<any> {
  throw new Error(
    "gamificationActions has been moved to app/actions/gamification.ts",
  );
}
